// RemoteAgent web client — M0 stub. M1 接入 xterm.js 终端。
function App() {
  return (
    <main style={{ fontFamily: 'monospace', padding: 32 }}>
      <h1>RemoteAgent</h1>
      <p>web client skeleton (M0) — terminal lands in M1.</p>
    </main>
  );
}

export default App;
